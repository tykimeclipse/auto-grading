# orphan 정리 배치 스크립트 (scripts/cleanup-orphans.js) 전용 환경변수.
#
# ⚠️ service_role key 는 DB 전권을 가진다.
#    이 파일(.env.cleanup)은 배치 스크립트에서만 로드하며,
#    프론트엔드 / 파일서버 본체 / git 어디에도 노출하지 않는다.
#    (.gitignore 에 .env.cleanup 등록됨)

SUPABASE_URL=https://gspsquuyqkydqphbcuel.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE_ME_service_role_key

# 파일서버 본체의 .env 와 동일한 값으로 맞출 것
DATA_DIR=./data/mistake-notes
